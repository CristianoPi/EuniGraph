# Migrations

Alembic migration scripts live in `versions/`.

The migration environment is intentionally included from the start so that schema evolution stays explicit and reviewable as the domain model grows.
