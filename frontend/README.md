# Frontend

The frontend is intentionally kept as a workspace placeholder in Issue 1.

Rationale:
- keep the monorepo shape stable from the start
- allow future UI work without repository reshuffling
- avoid committing to a web stack before backend contracts and visualization needs are clearer

At this stage the directory exists to reserve the boundary, not to implement the final UI.
